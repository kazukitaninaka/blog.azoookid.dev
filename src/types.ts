export type Tag = {
  id: string;
  name: string;
};

export type Block = {
  id: string;
  type: string;
  text: string;
};

export type Article = {
  id: string;
  title: string;
  createdAt: string;
  thumbnail: string;
};
